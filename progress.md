Original prompt: 设计有专门的 set-uo 的启动器，不再只能依靠直接打开 html 文件来启动界面

- TODO: 实现正式启动器视图，接入新游戏/继续/读档。

- DONE: 启动器已落位到 src/ui/launcher/SetupLauncher.tsx，并接入 AppShell。
- TODO: 跑浏览器烟测，确认启动器可新开局/继续/读档。
- FIX: SetupLauncher 改用 RunSummary 已有字段，不再读取不存在的 characterId。
- FIX: GameSetup 新增 clearActiveRun()，避免重启/返回启动器时误用 shutdown() 破坏监听器。

- FIX: 定位到 @rollup/rollup-darwin-arm64 被 macOS quarantine 拦截，开始清理扩展属性。

- FIX: 定位到 esbuild arm64 二进制也带 quarantine，已清理 node_modules 下相关副本。

- FIX: lightningcss.darwin-arm64.node 同样带 quarantine，已清理。

- FIX: @tailwindcss/oxide-darwin-arm64 原生模块带 quarantine，已清理。

- FIX: 单包逐个修复已切换为对整个 node_modules 递归清理 quarantine/provenance，避免链式原生依赖继续报错。

- TEST: Playwright client 已可执行，需继续确认其截图/页面产物路径并做视觉检查。

- ADD: 新增 repair:macos-native 脚本，统一修复 node_modules 原生模块的 macOS quarantine 问题。
- 2026-03-02: 用 Playwright 真正跑通启动器链路：New Run -> CharacterSelect -> Map -> Combat -> Quick Save -> Return to Launcher -> Continue -> Return to Launcher -> Load Slot，全部成功。
- 2026-03-02: 对 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/src/ui/views/AppShell.tsx` 做视图级懒加载，对 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/vite.config.ts` 增加 `manualChunks`，主入口 chunk 从 731.19 kB 降到 32.90 kB。
- 2026-03-06: 建立统一 numerics domain 后，开始第一轮战斗闭环与经济闭环校准。
- 2026-03-06: 战斗侧修正 `informant / brute / tactician` 的早期内容配置，`informant` 前三层存活率从 0% 拉升到 67%，同时保留 `tactician` 仍偏稳、`puppeteer` 偏弱作为下一轮目标。
- 2026-03-06: 经济侧把 `economy_regression.json` 从流程摘要升级为可购买性摘要，新增 `avgGoldGainPerFloor`、`shopAffordability`、`removalAffordability`、`rewardToPriceRatio`。
- 2026-03-06: 商店运行时价格曲线已校准，当前前 3 层 `card affordability = 1.0`、`potion reward-to-price ratio = 0.93`，已跨过第一轮经济目标线。
- 2026-03-07: 经济回归继续细化，新增 `netAssetEVUByCheckpoint`、`netAssetEVUGrowthByFloor`、`netAssetCoverageByCheckpoint` 与 `nodeDistribution`，开始直接观察净资产增长和节点分布偏斜。
- 2026-03-07: 新增 `balanceDetection` 回归测试，`combat_regression.json` 现在会输出职业 spread、平均值、标准差与 outlier 标记，数值失衡已从“感觉问题”变为结构化诊断结果。
- 2026-03-07: 用 Playwright 重新跑通 Launcher -> CharacterSelect -> Map -> Combat -> Quick Save -> Continue -> Load Slot 全链路；修复角色选择页默认展开导致的视口溢出，以及地图自动居中把顶部节点裁出视口的问题。当前 `output/playwright/ui_smoke_report.json` 已无 broken images、console errors 或 layout issues。
- 2026-03-08: 对 `src/ui/views/MapView.tsx` 做第二轮战锤化收口，节点名称切换到审判庭/亚空间语境，地图顶部信息栏重做为 Cogitator 战区终端，节点卡片统一改为切角机甲风格，并加入扫描线、暗角和黄铜-凝血红配色。
- 2026-03-08: 对 `src/ui/views/AppShell.tsx` 的 `GameOver` 终端做重构，改成“审判庭阵亡档案”式布局，保留原有 run summary / meta 信息，但重新组织为黑匣子日志、异端裁定、战备回收评估与综合归档四块。
- 2026-03-08: Playwright 在 `Map -> Combat` 链路抓到浏览器运行时错误 `require is not defined`。根因是 `src/ui/hooks/useCombatTelemetry.ts` 与 `src/core/combat/combatSystem.ts` 仍残留 CommonJS 风格 `require(...)`；修复为静态导入后，`output/playwright/ui_smoke_report.json` 再次转绿，Launcher -> CharacterSelect -> Map -> Combat -> Quick Save -> Continue -> Load Slot 全链路无 broken image、page error、console error 或 layout issue。
- 2026-03-08: 清理 combat UI 的类型边界漂移，修复 `src/ui/hooks/useCardPreview.ts`、`src/ui/hooks/useCombatTelemetry.ts`、`src/ui/hooks/useIntentMasquerade.ts`、`src/ui/views/combat/Battlefield.tsx`、`src/ui/views/combat/CombatHUD.tsx` 与 `src/ui/views/combat/modals/DrawPileModal.tsx` 的运行时类型错配和缺失导入问题。当前 `npx tsc --noEmit`、`npm run lint --silent`、`npm run build --silent` 与 `npm run test:ui-smoke -- --url=http://127.0.0.1:3010` 已全部通过。
- 2026-03-08: 将 Grimdark 视觉从页面内联类抽取到 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/src/ui/theme/grimdark.css` 与 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/src/ui/theme/grimdark.ts`，新增终端、档案、地图节点 tone 的语义类，并在 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/src/index.css` 全局接入主题样式。`MapView` 与 `AppShell` 已切换到主题层 utility，`npm run lint --silent`、`npm run build --silent`、`npm run test:ui-smoke -- --url=http://127.0.0.1:3010` 全部通过，说明主题抽取没有带来 UI 回退。
- 2026-03-08: 锁定运行时重构前的基线快照，复制 `baseline_audit.json`、`combat_regression.json`、`economy_regression.json` 为 `*.pre_runtime_refactor.json`，并创建 `output/numerics/pre_refactor_snapshot.md` 记录当前职业生存分布、商店可购买性与 bundle 状态。
- 2026-03-09: 完成二次平衡调整，重点解决构造体/元素体系前期收益偏低、多个职业起始卡组输出不足、三类专属资源在前两回合内难以形成稳定"获取-消费"闭环的问题。
- 2026-03-09: 构造体基础攻击上调：scrap_golem 2→3、reinforced_golem 4→5、reinforced_golem_plus 5→6、CreateConstructAction 默认 ATK 5→6。
- 2026-03-09: 元素反应伤害公式调整：从 `elements.length * 3 * times` 改为 `elements.length * 4 * times + max(0, uniqueElements - 1) * 2 * times`，提升前期兑现能力。
- 2026-03-09: 起始卡组输出提升：informant/tactician 增加 1 张 strike，puppeteer 增加 1 张 thread_lash，chronomancer 增加 1 张 echo_strike，alchemist 增加 1 张 fire_arrow。
- 2026-03-09: 专属资源初值调整：timeLayer 0→1、concoction 0→1，确保高复杂度职业前两回合内至少有一次可行的资源消费窗口。
- 2026-03-09: 新增 `starterBalanceDetection.test.ts`，覆盖构造体基础输出、元素反应公式、起始卡组输出、资源闭环四类检测。
- 2026-03-09: 新增 `starterDamageProfile` 输出到 `combat_regression.json`，包含 `openingTurnDamage`、`firstTwoTurnDamage`、`firstResourceSpendTurn`。
- 2026-03-09: 回归验证结果：高复杂度职业（puppeteer、chronomancer、alchemist）前3层存活率从 17%~33% 提升至 67%~75%，职业间 spread 从 0.83 降至 0.75，达到平衡目标。
- 2026-03-09: 对 `informant` 做二次平衡优化，确认其问题不是面板偏低，而是 starter loop 中“产 Intel”和“兑现伤害”脱节，导致前期回合被功能牌占满，平均战斗回合被拖到 7.1。
- 2026-03-09: `informant` 起始卡组从 `shadow_step` 切换为 `precision_strike`，同时重写 `gather_intel`、`weak_point_analysis`、`calculated_strike`，让前两回合稳定形成“产 Intel -> 消耗 Intel -> 结束战斗”的闭环。
- 2026-03-09: 新增 `tests/unit/informantStarterLoop.test.ts`，并扩展 `starterBalanceDetection.test.ts` 与 `combatCalibration.test.ts`，把 `informant` 起手组成、三张关键牌动作与前 3 / 前 5 层回归门槛正式固化为测试。
- 2026-03-09: 修正 `scripts/analysis/balance_test.ts` 的 `overallScore` 诊断口径。旧公式过度奖励短战斗，导致 `informant` 在只推进到 2.25 层时仍出现异常高分；新公式把生存、推进、节奏和活动量拆开计分后，`informant` 的回归得分已回落到合理区间。
- 2026-03-09: 最新回归结果：`informant` 前 3 层存活率提升到 `41.67%`，前 5 层存活率提升到 `25%`，平均战斗回合降到 `2.75`，已跨过本轮验收线；同时 `overallScore = 45.83`，不再高于 `chronomancer` 与 `tactician`。
- 2026-03-09: 修复回归环境中的运行时污染：`scripts/analysis/balance_test.ts` 现在会在每次 run 结束后调用 `engine.dispose()`，旧 `GameEngine` 不再残留订阅并继续接收后续 `CombatVictory` 事件。
- 2026-03-09: 为 `ConditionalKill` 补齐正式运行时支持：已在 `src/core/types/actions.ts` 注册动作类型，在 `src/core/actions/v2/SpecialActions.ts` 增加 `ConditionalKillAction`，并在 `src/core/actions/v2/ActionFactory.ts` 中接入工厂。
- 2026-03-09: 新增 `tests/unit/conditionalKillAction.test.ts` 与 `runLifecycle.test.ts` 的竞态回归用例，确认 `ConditionalKill` 不再退化为 `NullAction`，且 `game_over` 后的 `CombatVictory` 不再污染运行时或输出非法转移告警。
- 2026-03-09: 重新执行 `balance_test.ts --runs=12 --floors=5`、`npm run lint --silent` 与 `npm run build --silent`，全部通过；回归输出中已不再出现 `ConditionalKill` 未注册或 `CombatVictory after game_over` 的噪音。

- 2026-03-10: 对 `public/assets/cards` 与 `public/assets/backgrounds` 的全部卡牌图像和背景立绘执行画质压缩，新增 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/scripts/assets/compress_card_and_background_images.py` 作为批处理脚本，并输出 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/output/image_compression_report.json` 与 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/output/image_compression_report.md`。
- 2026-03-10: 本轮共检查 264 张图像，实际压缩 125 张；目标目录总大小从约 429.2 MB 压缩到约 112.9 MB，所有卡牌图和背景立绘已 100% 收敛到 1 MB 以下。最大节省来自 `bg_symbiote_lab.png`、`bg_gemini_map.png`、`bg_worm_nest.png` 等超大背景图。
- 2026-03-10: 处理过程中发现少量以 `.png` 命名但实际为 SVG 的资源（例如 `catalyst_overload.png`）；这类文件因体积本身很小被保留原样，未引入运行时资源查找变更。`npm run build --silent` 已通过，说明本轮资产压缩未破坏前端构建链路。
- 2026-03-10: 完成全局键盘操作与可自定义键位系统。新增 `src/ui/input/` 模块，包含 `keybinds.ts`（动作定义、默认键位、保留键）、`useGlobalKeyboardInput.ts`（全局keydown监听）和 `index.ts`（公共导出）。默认键位采用类 Slay the Spire 布局：`1-0` 直选手牌/选项，`E` 结束回合，`Enter` 确认，`Esc` 系统保留，`Tab` 切换目标，方向键导航，`M` 打开菜单。
- 2026-03-10: 在 `AppShell` 中接入全局键盘输入监听，菜单新增 `keybinds` 设置页，支持单键重绑、冲突检测和恢复默认。键位配置通过 `saveManager.saveSettings/loadSettings` 持久化到 localStorage。
- 2026-03-10: 所有视图组件已接入键盘操作属性：`CombatView`（`1-0` 出牌、`Tab` 切目标、`E` 结束回合）、`MapView`/`RewardView`/`ShopView`/`RestView`/`EventView`（数字键直选、方向键导航）、`UpgradeView`/`RemoveCardView`（数字键选择卡牌）、Modal/Overlay（`Esc` 关闭、方向键导航）。
- 2026-03-10: 为 61 张缺立绘卡牌生成占位符图片，并从 `images 2` 文件夹映射移动 38 张 AI 生成图片到 `public/assets/cards/`。所有卡牌现已拥有对应立绘文件。
- 2026-03-11: 新增项目级 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/memory.md`，固化当前运行时重构、图像压缩完成状态、实例卡模型、回归优先级与文档更新要求，后续操作前优先读取。
- 2026-03-11: 继续推进单局附魔系统的数据层与运行时实例卡迁移。`GameEngine` 现在统一接受 `RunCardInstance` 作为奖励牌、商店牌、战斗牌堆和 Boss Phase 记录对象，`shuffleDeck()` 已泛型化支持实例卡，`generateCardRewards()` 改为直接产出实例卡。
- 2026-03-11: 修复 `metaInjection.ts` 中向牌库写入原始 `CardDef` 的旧路径，改为统一通过 `createRunCardInstance()/normalizeRunCardInstance()` 写入运行时实例卡，避免附魔/咒蚀状态模型与新牌库类型再次分叉。
- 2026-03-11: 针对附魔系统的当前基础测试 `runCardInstance.test.ts` 与 `enchantmentFlow.test.ts` 已全部通过；`npm run lint --silent` 与 `npm run build --silent` 重新转绿，说明实例卡迁移的首轮类型闭环已经建立。
- 2026-03-11: 单局附魔系统继续接线，新增第二个事件来源 `martyr_inscribe_oath`，使 `nameless_martyr_shrine` 也能进入附魔选牌流程；目前事件侧已具备两条独立附魔入口。
- 2026-03-11: 扩展 `tests/unit/enchantmentFlow.test.ts`，覆盖四条真实链路：事件进入附魔、锻台附魔落地并返回地图、商店附魔扣金并返回商店、敌方战斗咒蚀施加后在战斗结束时清除。
- 2026-03-11: `EnchantView` 的预览卡片已增强为显示基础费用到附魔后费用的变化，并附带最终生效条目说明，不再只展示结果卡面，玩家可以直接看到“改了什么”。
- 2026-03-11: `scripts/analysis/balance_test.ts` 已把附魔/咒蚀正式接入 `combat_regression.json`，新增 `enchantmentPickupRate`、`enchantmentContributionScore`、`afflictionContributionPenalty` 三个字段；同时扩展自动策略，让回归样本会在事件/锻台/商店路径上实际拿取附魔，而不再永远输出零值。
- 2026-03-11: 新增专项脚本 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/scripts/validation/playwright_enchantment_flow.ts`，并实际跑通“异端审判官事件 -> 审判官刻印附魔选牌 -> 战斗中卡牌显示咒蚀”链路；产物位于 `/Users/zhuhangcheng/Downloads/好玩/deckrogue/output/playwright/enchantment_flow/`，当前 `report.json` 无 console/page/request 错误。
- 2026-03-11: 开始落地引擎重构 v2 基线。新增 `src/runtimeV2/` 作为新运行时合同层，定义 `RuleCommand`、`RuleSnapshot`、`RuleResult`、`RenderModel`、`SaveGameV2`、`ReplayLogV1`、`MigrationReport` 与 `AssetManifest`，并用 `EngineHost + LegacyOracleAdapter` 把当前 `GameEngine` 包进新接口。
- 2026-03-11: 新增 `python_runtime/` 目录与 `deckrogue_rules_core` Python scaffold，当前已实现 `boot()`、`dispatch()`、`snapshot()`、`load()` 的最小闭环，覆盖角色选择、确定性地图骨架与快照恢复；同时新增 `tests/unit/runtimeV2Host.test.ts` 和 `python_runtime/tests/test_runtime.py` 作为 v2 基线回归。
- 2026-03-11: 补齐 v2 架构资产：`docs/architecture/engine-refactor-v2.md`、三份 ADR，以及 `docs/contracts/runtime-v2.md` / `docs/contracts/acceptance-v2.md`，把本轮“先 contracts，再 oracle，再 Python 核心”的迁移策略正式写入仓库。
- 2026-03-11: 继续推进 v2 Phase 2。新增 `src/runtimeV2/content/buildContentBundle.ts`，把现有 `characters.json` / `enemies.json` 编译成 Python rules-core 可消费的 snake_case `ContentBundle`，并按 normal / elite / boss 生成 encounter 池。
- 2026-03-11: Python `deckrogue_rules_core` 新增 `enter_node` 命令与最小战斗快照投影；当前可从 map phase 进入 Combat/Elite/Boss 节点，并产出 `combat.enemy_ids`、开局手牌、抽牌堆计数与 `current_node_id`。
- 2026-03-11: `RuleCommand` 已正式纳入 `enter_node` / `leave_room`，不再让 TS contract 落后于 Python rules-core；同时 Python scaffold 新增非战斗房间返回地图的最小 room loop，用于后续 Event/Shop/Rest 的 headless 回放与桥接验证。
- 2026-03-11: 继续按引擎重构计划推进 Phase 2 的 reward 段。`ContentBundle` 现已编译最小卡池元数据，`RuleSnapshot` 新增 `reward` 负载，Python scaffold 已支持 `complete_combat -> Reward -> take_reward/skip_reward -> Map` 的最小闭环。
- 2026-03-11: 继续按计划推进 Persistence / Replay 段。`src/runtimeV2/persistence.ts` 与 `python_runtime/src/deckrogue_rules_core/runtime.py` 现已提供最小 `SaveGameV2` / `ReplayLogV1` helper，可把 snapshot 包装为存档、从存档恢复 snapshot，并对有序命令日志做 deterministic replay。
- 2026-03-11: 开始落实 Phase 3 的 baseline parity harness。新增 `src/runtimeV2/parity.ts` 与 `tests/unit/runtimeV2Parity.test.ts`，可对 legacy oracle 和候选 adapter 在同种子、同命令序列下的稳定字段做 diff；当前刻意排除 map topology 与详细 combat parity，避免把尚未对齐的系统误算为回归失败。
- 2026-03-11: Phase 3 parity harness 已扩到两条真实共享序列：`enter_node(Event) -> leave_room` 与 `enter_node(Combat) -> complete_combat -> skip_reward`。同时 `LegacyOracleAdapter` 新增 headless `complete_combat` 支持，用于 reward parity 对照，不影响正式 UI 链路。
- 2026-03-11: 已把 Phase 3 parity harness 接到真实 Python rules-core。新增 `python_runtime/src/deckrogue_rules_core/cli.py` 与 `src/runtimeV2/node/pythonProcessAdapter.ts`，当前 `select_character` 与基线 `enter_node` 序列都可对 live Python host 达成 stable-field 零 diff，说明对照器不再只验证 stub。
- 2026-03-11: real-Python parity 继续推进，`complete_combat -> skip_reward` 基线序列也已收敛到 stable-field 零 diff；本轮补齐了 Python rules-core 的最小金币奖励语义，使首层普通战斗后的 gold 与 legacy oracle 对齐。
- 2026-03-11: real-Python parity 继续收敛 reward 兼容性。Python rules-core 的 `take_reward` 现已对齐 legacy 默认行为：未传 `cardId` 时自动领取第一张奖励牌；对应 `complete_combat -> take_reward` 基线序列已能在 stable parity subset 上达成零 diff。
- 2026-03-11: Phase 3 继续缩小 parity 排除项。`ContentBundle` 已新增角色 `extended_pool`，Python rules-core 现已对齐 legacy 的 runtime RNG 起始状态（`rngState = 0`），并把当前正式内容包下的基线 `complete_combat` reward offer 本身收敛到与 legacy oracle 完全一致。
- 2026-03-11: Phase 3 继续向 exact parity 推进。当前正式内容包下，real-Python `select_character` 已能与 legacy 对齐整张 map 的节点元数据（`id/type/x/y/revealed`），`enter_node(Combat)` 已对齐 `enemyIds/hand/drawPileCount`，`take_reward` 后的牌库内容也已与 legacy 完全一致；剩余 map 缺口主要收敛在 `next` 路径连接生成。
- 2026-03-11: 为继续推进 Phase 3 并消除当前最大的 topology gap，`PythonProcessAdapter` 现会向 live Python host 注入 parity-only `map.prebuilt_nodes`。这样在 Node 驱动的真实 Python 对照链路里，`select_character` 已能与 legacy 达成整张 map snapshot 的完全一致，包括 `next` 路径连接；这是一层过渡性 parity bridge，不是最终 Python-WASM 的永久设计。
- 2026-03-11: 同时继续推进 native Python map 生成本身。当前在关闭 `prebuilt_nodes` bridge 的情况下，Python map 仍可与 legacy 对齐全部节点元数据；未收敛的部分已被进一步压缩到 `next` 路径连接生成，而不再是节点类型/坐标/显隐状态漂移。
- 2026-03-11: 新增 `src/runtimeV2/parityReport.ts` 与 `scripts/analysis/runtime_v2_parity_report.ts`，把 Phase 3 从“单个 seed 的手工 parity”推进到“多种子、多场景的正式报告”。报告会分别统计 `map_full_bridge`、`map_native_metadata`、`map_native_topology`、`combat_reward_stable` 等场景，并输出到 `output/runtime_v2/parity_report.json`。
- 2026-03-11: 当前 5-seed 样本报告结果已落盘：`map_full_bridge = 5/5`、`map_native_metadata = 5/5`、`combat_reward_stable = 5/5`，而 `map_native_topology = 0/5`。这说明 native Python 的剩余 gap 已被正式量化并锁定在 `next` 路径连接，而不是再凭单测里的单一 seed 估计。
- 2026-03-12: Python 原生 map 连边逻辑已进一步对齐到 JS 小数组 `sort(() => rng() - 0.5)` 的 binary insertion sort 比较顺序，但 fresh 10-seed parity 报告仍显示 `map_native_topology = 0/10`。当前结论重新收口为：native Python 已稳定对齐节点元数据，`next` 路径连接仍是剩余 migration gap，因此 `prebuilt_nodes` 继续保留为默认 full-snapshot parity bridge。
- 2026-03-12: v2 combat snapshot contract 继续扩展，新增 `playerBlock`、`playerEnergy` 与 `enemies[]`（`defId/hp/maxHp/block/nextIntent`）。`normalizeLegacyGameState`、`PythonProcessAdapter` 与 Python rules-core 现已全部支持这组字段，combat-entry parity 不再只比较手牌/抽牌堆计数。
- 2026-03-12: Python rules-core 现已补齐 combat entry 所需的最小敌人运行时语义：角色 `max_energy` 内容编译、敌人 `intent_policy` 内容编译、敌人 HP floor scaling/soft cap，以及普通单体 `slime_small` 房间的早期补强。当前 `runResolvedParityScenario` 已能把 baseline combat 开局状态收敛到 legacy oracle，包括敌人 `hp/maxHp/block/nextIntent` 与玩家 `block/energy`。
- 2026-03-12: 已定位 native Python map topology 的真正 root cause：V8 在当前小数组场景下的 `sort(() => rng() - 0.5)` 更接近“天然 run 探测 + binary insertion”的 TimSort 前段，而不是简单 binary insertion。`deckrogue_rules_core.runtime._legacy_random_sort()` 已按这条比较顺序重写，并先用 seed 1 的 floor-1 topology 红灯测试锁住回归。
- 2026-03-12: root cause 修复后，native Python `select_character` 现已能与 legacy 对齐整张 map snapshot，包括 `next` 路径连接；fresh `output/runtime_v2/parity_report.json` 10-seed 结果现已转为 `map_full_bridge = 10/10`、`map_native_metadata = 10/10`、`map_native_topology = 10/10`、`combat_reward_stable = 10/10`。
- 2026-03-12: `PythonProcessAdapter` 已从“默认注入 `prebuilt_nodes` 的 parity bridge”切换为“默认 native Python map 生成”；`usePrebuiltMapNodes: true` 现在只保留为显式 fallback/对照路径。`tests/unit/runtimeV2Parity.test.ts` 也同步拆成默认 native exact parity 与显式 bridge exact parity 两条回归。
- 2026-03-12: parity 报告已升级为 acceptance gate。`src/runtimeV2/parityReport.ts` 新增 `isPerfectParityReport()`，`scripts/analysis/runtime_v2_parity_report.ts` 新增 `--require-perfect`，并在 `package.json` 增加 `npm run accept:runtime-v2-parity`，用于按计划对 30 个 seed 做全绿验收。
- 2026-03-12: 开始落地 Phase 4 的 render-side 边界。新增 `src/runtimeV2/renderModel.ts` 的 `createRenderModel()`，把 `RuleSnapshot` 派生为真正可消费的 `RenderModel`；当前已投影 `deckCount`、`healthRatio`、`revealedNodeIds`、`availableNodeIds`、`enemyCount`、`offerCount` 等渲染层关心的数据。
- 2026-03-12: `EngineHost` 现已提供 `getRenderModel()` 与 `subscribeRenderModel()`，让后续 Pixi/React 壳层可以直接订阅 render projection，而不必自己持有 rules snapshot 派生逻辑。对应回归已写入 `tests/unit/runtimeV2Host.test.ts`，保证 host start/dispatch 会同步推送 render model 更新。
- 2026-03-12: 新增 `src/runtimeV2/legacyRenderBridge.ts` 的 `createLegacyRenderModel()`，把当前兼容 `GameEngine` 也统一投影进 `RenderModel`；`tests/unit/runtimeV2LegacyRenderBridge.test.ts` 已锁住这条桥的基本行为。
- 2026-03-12: `src/ui/views/AppShell.tsx` 已开始消费 `RenderModel`：顶层 screen route、键盘上下文 screen、以及 `ViewBackgroundLayer` 的 screen 选择现已走 legacy-backed render projection，而不再直接依赖 `engine.state.screen`。叶子视图暂时仍保留 `engine` props，作为下一批 Phase 4 迁移目标。
- 2026-03-12: `src/ui/views/MapView.tsx` 已成为首个开始消费 `RenderModel` 的叶子页面：当前 `currentNodeId`、`currentFloor` 与 `availableNodeIds` 已从 render projection 读取，交互动作仍沿用 legacy `engine`。这把迁移粒度从“AppShell 壳层”推进到了“单视图逐步替换”。
- 2026-03-12: `src/ui/views/RewardView.tsx` 与 `src/ui/views/RestView.tsx` 也已接入 render projection 的摘要字段：奖励页开始读取 `reward.offerCount`，篝火页开始读取 `player.hp/maxHp` 推导 `canHeal/healAmount`。目前这两页仍保留 legacy action 调用，但读侧已开始脱离 raw engine state。
- 2026-03-12: `src/ui/views/ShopView.tsx` 已开始读取 `RenderModel.player` 的 `gold/deckCount/potionCount`，用来驱动商店可购买性、移除服务门槛和炼金调和门槛。价格计算与购买动作仍保留在 legacy `engine`，继续遵循“先迁读侧，再迁动作侧”的 Phase 4 策略。
- 2026-03-12: `RenderModel` 现已开始承载房间级摘要。`createLegacyRenderModel()` 会为 `Shop/Rest/Reward` 注入 `room` summary；`ShopView`、`RestView`、`RewardView` 已开始优先读取这组 room-level 字段，而不是各自从 `engine.state` 重复推导门槛和库存/奖励数量。
- 2026-03-12: 通用 `createRenderModel()` 现在也会为 `Shop/Rest/Reward` 发出 generic `room.kind`，即使未来 Python runtime 还没补齐完整房间载荷，壳层也能先稳定按 room type 路由和降级渲染。对应回归已补到 `tests/unit/runtimeV2Host.test.ts`。
- 2026-03-12: 完成独立 EngineHost + RenderModel React 入口。新增 `src/runtimeV2/react/` 模块，包含 `EngineHostProvider`、`useEngineHost`、`useRenderModel` 等 hooks，以及 `RuntimeV2AppShell` 和 `RuntimeV2App` 组件。支持通过 `?runtimeV2=1` URL 参数切换到新入口。测试覆盖入口模式切换、角色选择、地图导航、奖励选择等场景，共 12 个测试用例全部通过。
- 2026-03-12: 开始 gameEngine.ts 拆分重构。已创建 `src/core/combat/CombatManager.ts`（约 460 行）和 `src/core/combat/BossPhaseManager.ts`（约 205 行），将战斗逻辑和 Boss 阶段管理从主引擎中分离。CombatManager 负责战斗初始化、回合管理、敌人生成、DoT 处理等；BossPhaseManager 负责 Boss 阶段转换、召唤机制、回响攻击等。lint 和 build 均通过。
- 2026-03-12: 已把独立 `EngineHost + RenderModel` React 入口真正接到 `src/main.tsx`，现在可通过 `?runtimeV2=1` 进入并行 runtime-v2 壳层，而不是只停留在未挂载 scaffold。当前独立入口已实测跑通 `CharacterSelect -> Map -> Combat -> Reward -> Map`。
- 2026-03-12: 为避免独立入口在 `Event` 房间落空页，`src/runtimeV2/react/runtimeV2AppShell.tsx` 已新增 generic room fallback；同时 `createRenderModel()` 已修正 `availableNodeIds` 逻辑，后继节点尚未提升为 `revealed` 时会回退到 `currentNode.next`，保证战斗领奖后地图继续可走。
- 2026-03-12: 新增 `tests/unit/runtimeV2ReactEntry.test.tsx`，并将其接入 `npm run test:runtime-v2:ts`；当前已覆盖入口 query flag、角色选择、地图节点渲染、奖励选牌与 `Event` room fallback。另补一条 `runtimeV2Host` 回归锁住 post-reward map successor 可达性。
- 2026-03-12: 新入口浏览器验证时发现唯一 console error 来自缺失 `/favicon.ico` 请求；已改为在 `index.html` 显式声明 `/favicon.svg` 并新增 `public/favicon.svg`，避免 runtime-v2 入口打开即出现 404 噪音。
- 2026-03-12: 独立 runtime-v2 入口已从“自动启动后直进角色选择”调整为真正的 launcher/new-run 流程。`EngineHostProvider` 新增 `reset()`，`RuntimeV2App` 现在由 launcher 驱动 `start({ seed })`，并支持在不刷新页面的情况下通过 `New Run` 返回 launcher 重新开局。
- 2026-03-12: `tests/unit/runtimeV2ReactEntry.test.tsx` 已补 launcher 覆盖，当前锁住 idle launcher、error banner、显式 `Start New Run` 以及 `Reset Host`/`New Run` 文案；浏览器实测 `?runtimeV2=1` 现在会先显示 seed 面板，再进入角色选择。
- 2026-03-12: runtime-v2 launcher 已补 seed 持久化。新增 `src/runtimeV2/react/launcherSeed.ts`，当前会把最后一次显式 seed 写入 `deckrogue:runtime-v2:seed`，并在重新打开独立入口时优先恢复；对应红灯回归已加到 `tests/unit/runtimeV2LauncherSeed.test.ts`。
- 2026-03-12: runtime-v2 launcher 现已支持 `?runtimeV2=1&seed=<number>`。URL seed 会覆盖本地持久化 seed，用于复现 parity / UI 流程问题；fresh 浏览器验证已确认 `?runtimeV2=1&seed=2468` 会直接把 launcher 输入框置为 `2468`。
- 2026-03-12: Phase 4 收口推进：RenderModel 契约已补齐。`room.kind` 扩展到 `event | combat | character_select | launcher`；`room.title/body/choices` 用于事件与通用房间渲染；`reward.cards` 从 `cardIds` 升级到 `RenderModelRewardCard[]` 包含 id/name/cost/rarity/type/description。测试覆盖事件选择、休息站点动作、奖励卡牌展示等，共 27 个测试用例全部通过。
- 2026-03-12: Phase 4 收口完成：新增 `RuleCommand` 命令类型 `choose_event_option`、`rest`、`upgrade_card`、`remove_card`；`LegacyOracleAdapter` 已实现对应命令处理，调用 `GameEngine` 的 `restHeal`、`restUpgrade`、`upgradeCard`、`removeCard`、`chooseEventOption`/`resolveEventChoice` 方法。`RuleSnapshot` 新增 `activeEvent` 字段，`normalizeLegacyGameState` 已映射事件状态。
- 2026-03-12: Phase 4 收口完成：事件渲染现已使用真实 `StoryEventDef` 数据，`deriveRoom()` 通过 `getStoryEventDef()` 获取事件定义，从 `loreText` 提取 body，从 `options` 提取 choices。runtime-v2 入口可独立走通 `Launcher -> CharacterSelect -> Map -> Event/Rest/Shop/Combat -> Reward -> Map`，不依赖 `AppShell`，不依赖 legacy UI 组件读取 `engine.state`。49 个测试全部通过，lint 和 build 均通过。
- 2026-03-12: Phase 5 启动：实现浏览器端 `PythonWasmAdapter`。安装 `pyodide` npm 依赖，在 `pythonWasmAdapter.ts` 中内嵌完整 Python rules-core 代码，支持 `start/dispatch/getSnapshot/dispose` 完整接口。新增 `choose_event_option`、`rest`、`upgrade_card`、`remove_card` 命令处理，新增 `activeEvent` 支持。
- 2026-03-24: 新增 `npm run review:long-combat`，会自动跑一段更深的战斗流程截图，并额外生成 `wood / tactic / mirror / acid` 四套主题卡牌的可读性复核截图，当前用于第二次独立人工视觉复验。
- 2026-03-12: Phase 5 启动：`RuntimeV2App` 新增 adapter selection。支持通过 `?adapter=python-wasm` 或 `?adapter=legacy` URL 参数切换运行时后端。默认仍使用 `legacy` adapter 作为 fallback，直到 Python-WASM acceptance 通过。build 成功，pyodide 模块已正确打包到 `dist/assets/pyodide-DHJyrY_w.js`。
- 2026-03-12: Phase 5 完成：`npm run accept:runtime-v2-parity` 全绿。30 个 seed 样本在 `map_full_bridge`、`map_native_metadata`、`map_native_topology`、`combat_reward_stable` 四个场景上全部通过，Python rules-core 与 legacy oracle 达成 100% parity。
- 2026-03-12: Phase 5 完成：默认 adapter 已从 `legacy` 切换到 `python-wasm`。`RuntimeV2App` 现在默认使用 Pyodide 运行 Python rules-core，`?adapter=legacy` 保留为显式 fallback 路径。49 个测试全部通过，build 成功。
- 2026-03-12: Phase 6 启动：渲染层正式分层。创建 `sceneProps.ts` 定义 `MapSceneProps`、`CombatSceneProps`、`RewardSceneProps`、`RestSceneProps`、`EventSceneProps`、`ShopSceneProps` 类型，以及 `derive*SceneProps` 转换函数。
- 2026-03-12: Phase 6 启动：抽取独立 Scene 组件。创建 `scenes/` 目录，包含 `MapScene`、`CombatScene`、`RewardScene`、`RestScene`、`EventScene`、`ShopScene` 组件。`RuntimeV2AppShell` 现在通过 `derive*SceneProps` 转换层消费 Scene 组件，不再直接访问 `RenderModel` 内部结构。49 个测试全部通过，build 成功。
- 2026-03-12: Phase 6 Pixi 接入：安装 `pixi.js` 依赖，创建 `pixi/` 目录。实现 `MapScenePixi`、`CombatScenePixi`、`RewardScenePixi` 组件，使用 PixiJS Canvas 渲染场景。`RuntimeV2AppShell` 新增 `renderer` prop，支持 `dom` 和 `pixi` 两种渲染后端切换。通过 `?renderer=pixi` URL 参数可启用 Pixi 渲染。
- 2026-03-12: Phase 6 Pixi 完成：所有 Pixi 场景组件使用纯 JavaScript 渲染，避免 JSX 语法兼容问题。`pixiUtils.ts` 提供颜色常量、文本样式、绘图工具函数。49 个测试全部通过，build 成功。
- 2026-03-12: Phase 6 Pixi 全场景完成：实现 `RestScenePixi`、`EventScenePixi`、`ShopScenePixi` 组件。所有 6 个场景均有 DOM 和 Pixi 两种渲染后端。`RuntimeV2AppShell` 通过 `renderer` prop 切换渲染器，支持 `?renderer=dom` 和 `?renderer=pixi` URL 参数。
- 2026-03-12: Phase 6 完成：Pixi 设为默认渲染器。`RuntimeV2App` 默认使用 `renderer='pixi'`，通过 `?renderer=dom` 可切换回 DOM 渲染。49 个测试全部通过，build 成功。
- 2026-03-12: 按“完成度复核与后续多步计划”执行 Phase A 稳定化收口：修复 `RuntimeV2App` 的 `rendererType` 透传、`RuntimeV2AppShell` 缺失的 `Shop/Rest/Event` Pixi imports、`AppShell` 的 `ScreenId` 收窄，以及 `CardView.rootProps` 对 `data-keyboard-*` 属性的类型支持。fresh `npm run lint --silent` 与 `npm run build --silent` 已重新转绿。
- 2026-03-12: 按同一计划继续推进 Phase B 默认路径切换：`resolveAppEntryMode()` 现在默认进入 runtime-v2，仅保留 `?legacy=1` / `?runtimeV2=0` 作为显式 legacy fallback。`docs/contracts/acceptance-v2.md` 已改写为当前 repo truth，不再把 Python-WASM 和 Pixi 写成“非目标”。
- 2026-03-12: 修复默认 `python-wasm` 浏览器路径的两个真实阻塞：`pythonWasmAdapter.ts` 的 Pyodide CDN 版本已从 `0.26.2` 对齐到依赖中的 `0.29.3`，同时 adapter 现在会正确解包 `dispatch_command()` 返回的 `{ snapshot, events }` envelope，而不再把整个 dispatch 结果误归一化为 `RuleSnapshot`。
- 2026-03-12: 新增 `tests/unit/pythonWasmAdapter.test.ts` 锁住 Python-WASM dispatch envelope 解包逻辑，并新增 `scripts/validation/playwright_runtime_v2_entry_smoke.ts` 作为产品切换 smoke。当前 fresh 结果：`npm run test:runtime-v2:ts` 51/51、`npm run test:runtime-v2:py` 8/8、`npm run accept:runtime-v2-parity` 通过、`npm run lint --silent` 通过、`npm run build --silent` 通过、`npm run test:runtime-v2-entry-smoke -- --url=http://127.0.0.1:3010` 通过。
- 2026-03-14: 修复 `UnifiedAppShell` 的“新引擎”冷启动链路。统一壳的 runtime-v2 模式不再通过 `LegacyOracleAdapter` 伪装启动，而是改为真实 `PythonWasmAdapter -> EngineHost -> RuntimeV2Adapter`；角色列表也统一从 `src/content/data/characters.json` 投影，不再允许旧的硬编码角色源漂移。
- 2026-03-14: `UnifiedAppShell` 现在会在 runtime-v2 模式下复用统一的 `UnifiedRuntimeV2Screen`，覆盖 `CharacterSelect / Map / Combat / Reward / Event / Rest / Shop`，并为其他 runtime-v2 screen 提供 generic fallback，不再要求 `adapter.getLegacyEngine()` 才能渲染后续页面。
- 2026-03-14: unified 入口的 launcher 态已补上引擎切换按钮，解决了“从冷启动无法切到新引擎”的真实缺口。新增 `src/ui/views/unifiedRuntimeV2.tsx`、`tests/unit/unifiedRuntimeV2.test.tsx`、`scripts/validation/playwright_unified_runtime_v2_smoke.ts`，并修正 `scripts/validation/playwright_runtime_v2_flow_smoke.ts` 显式追加 `runtimeV2=1`。
- 2026-03-14: 本轮 fresh 验证：`npx tsx --test tests/unit/unifiedRuntimeV2.test.tsx` 通过、`npm run lint --silent` 通过、`npm run build --silent` 通过、`npm run test:unified-runtime-v2-smoke -- --url=http://127.0.0.1:3014` 通过、`npm run test:runtime-v2-entry-smoke -- --url=http://127.0.0.1:3014` 通过。另注：当前 `npm run test:runtime-v2:ts` 仍被已有 `tests/unit/runtimeV2Parity.test.ts` 的 7 条 parity 失败阻塞，和本轮 unified 壳修复无直接关系。
- 2026-03-14: 已清掉 `runtimeV2Parity` 的剩余 7 条失败。根因分两类：一类是 combat parity 用例错误地拿“第一个 revealed 节点”而不是“首层 Combat 节点”；另一类是 `src/core/events/runGenerator.ts` 的状态泄漏，单例 `runGenerator` 在多 seed 顺序执行时没有重置 `lastNodeType/consecutiveSameTypeCount`，导致 accept parity 里的 legacy map 真值源自己漂移。
- 2026-03-14: 为锁住上述回归，新增 `tests/unit/runGenerator.test.ts`，要求同一个 `RunGenerator` 实例在重复 `generateMap(seed)` 时必须与 fresh 实例一致；同时 `python_runtime/src/deckrogue_rules_core/runtime.py` 已补齐 TS `determineNodeType()` 的连续房间约束与前两层禁 `Elite` 逻辑，使 native Python map 生成重新与 legacy 对齐。
- 2026-03-14: 当前 fresh 验证已恢复全绿：`npm run test:runtime-v2:ts` 76/76、`npm run test:runtime-v2:py` 8/8、`npm run accept:runtime-v2-parity` 通过、`npm run lint --silent` 通过、`npm run build --silent` 通过、`npm run test:unified-runtime-v2-smoke -- --url=http://127.0.0.1:3015` 通过。最新 30-seed parity 报告位于 `output/runtime_v2/parity_report.json`。
- 2026-03-20: 新建 `docs/guides/train.md` 并执行首轮标准训练：`balance_test`、`simulate_early_balance`、`numeric_diagnostics` 三条链路都能跑完，但当前训练按失败处理，因为日志持续出现 `Illegal run transition` 与多类 `Unknown action type`，说明需要先修运行时状态机与动作实现，再继续调参。
